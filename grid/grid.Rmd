---
title: "R Notebook"
---

```{r}
library(terra)

coast <- vect("inputs/WP3_grid_marine_polygon_v2/WP3_grid_marine_polygon_v2.shp")
coast <- project(coast, "EPSG:3035")
plot(coast)

grid <- rast(crs = crs(coast), extent = ext(coast), resolution = 250)
grid <- rasterize(coast, grid, touches = TRUE)
plot(grid)

writeRaster(grid, "outputs/grid_marine_250m_v2.tif", overwrite = TRUE)
```


